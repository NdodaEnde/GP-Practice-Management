[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto

markers =
    integration: needs running Supabase + microservice; cheap DB hits only (no LandingAI)
    slow_integration: needs full ingestion pipeline (LandingAI parse, real validation flow) — CI only
