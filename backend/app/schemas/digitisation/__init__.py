# app/schemas/digitisation/__init__.py
# Pydantic extraction schemas for the multi-industry digitisation pipeline.
# Per-industry files; the SCHEMA_REGISTRY in app.services.schema_registry
# binds (industry_type, doc_type) → schema for runtime lookup.
